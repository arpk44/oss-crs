crs-multilang:
  models: []
  # No dind dependency - uses host docker socket directly
  # Parent image is already on host daemon, no tarball save/load needed
  host_docker_builder: true
  modes:
    - full
    - delta
